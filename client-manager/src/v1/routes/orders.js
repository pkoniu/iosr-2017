const express = require('express');

module.exports = (ordersColl) => {
    const app = express();

    app.get('/', (req, res, next) => {

    });

    app.get('/:id', (req, res, next) => {

    });

    app.post('/', (req, res, next) => {

    });

    return app;
}